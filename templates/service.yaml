apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-svc
  namespace: helm

spec:
  selector:
    app: {{.Release.Name}}-web-page
  ports:
    - port: 7900
      targetPort: 7900
  type: {{.Values.application.service.type}}  
  
  